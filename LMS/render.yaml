services:
  - type: web
    name: bookpilot-manager
    env: java
    plan: free
    buildCommand: "javac -d out $(find src -name '*.java')"
    startCommand: "java -jar dist/BookPilotManager.jar"
    envVars:
      - key: PORT
        value: 99999
